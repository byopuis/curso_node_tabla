# NOTES

This is my first example/project in node.js

```
Options:
      --help     Show help                                   [boolean]
      --version  Show version number                         [boolean]
  -b, --base     it is the base of the multiplication
                                                   [number] [required]
  -l, --listar   Shows the list of operations
                                          [boolean] [default: "false"]  -h, --hasta    Put a limit of the number of operations that has to
                 be done                      [number] [default: "10"]

```
